import React, { Component } from 'react';

export default class EditExercise extends Component {
    render() {
        var text = "You are on the Edit Exercises component!";
        return(
            <div>
                <p>{text}</p>
            </div>
        )
    }
}